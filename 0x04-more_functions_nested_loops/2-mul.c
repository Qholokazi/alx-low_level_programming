#include "main.h"
#include <stdio.h>

/**
 * mul - Entry point
 * @a: - The first integer
 * @b: - The second integer
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}
